/**
 * Default authenticated landing view — intentionally empty.
 * Dashboard (quick links, cash balance) is reached by clicking the app logo.
 */
export function HomePage() {
  return <div className="min-h-[50vh]" aria-hidden="true" />;
}
